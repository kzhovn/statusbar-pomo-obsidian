import {Timer} from 'src/timer';
import {DEFAULT_SETTINGS} from 'src/settings';
import PomoTimerPlugin from 'src/main';
import * as jest from 'jest';

//no idea how to mock classes, let's leave this for now
describe ('Timer', () => {
    let timer: Timer;
    let plugin: PomoTimerPlugin;
    // jest.mock('src/main');


    // beforeEach(() => {
    //     timer = new Timer();
    // })
})

